spring:
  config:
    activate:
      on-profile: local

server:
  port: 8089

kafka:
  clusters:
    bootstrapservers: localhost:9092


---

spring:
  config:
    activate:
      on-profile: docker


kafka:
  clusters:
    bootstrapservers: lucycato-kafka:9092